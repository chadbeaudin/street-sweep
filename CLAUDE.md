# StreetSweep — Claude Project Instructions

## Server Logs
The dev server logs to `/tmp/streetsweep.log` (piped via `start.sh`).
To check logs: `tail -f /tmp/streetsweep.log` or `cat /tmp/streetsweep.log`.
